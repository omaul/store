{
  "name": "store",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Витрина колец ручной работы из латуни",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  }
}
